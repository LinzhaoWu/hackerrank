#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include <string>

int main() {
    std::string sLine;
    std::getline(std::cin, sLine);

    uint32_t nAttribute, nQuery;
    sscanf(sLine.c_str(), "%d %d", nAttribute, nQuery);

    return 0;
}
